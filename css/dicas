/* ==========================================================================
   DICAS.CSS — grade de mapas; cada card abre as dicas daquele mapa no
   torneio Tavernas (tavernas.html?mapa=NOME#page-maps).
   ========================================================================== */

.dicas-grid { display: grid; grid-template-columns: repeat(5, 1fr); gap: 14px; }
.dica-card {
    position: relative; aspect-ratio: 4/3; border-radius: var(--radius-sm); overflow: hidden;
    border: 1px solid var(--border-color); text-decoration: none; display: block;
    background-size: cover; background-position: center;
}
.dica-card::after {
    content: ''; position: absolute; inset: 0;
    background: linear-gradient(180deg, transparent 40%, rgba(0,0,0,0.85) 100%);
}
.dica-card span {
    position: absolute; left: 10px; bottom: 8px; z-index: 1;
    font-family: 'Cinzel', serif; font-size: 0.7rem; letter-spacing: 1px;
    text-transform: uppercase; color: var(--text-main);
}
.dica-card:hover { border-color: var(--hover-color); }
.dica-card:hover span { color: var(--brand-red-strong); }

@media (max-width: 980px) { .dicas-grid { grid-template-columns: repeat(3, 1fr); } }
@media (max-width: 600px) { .dicas-grid { grid-template-columns: repeat(2, 1fr); } }
